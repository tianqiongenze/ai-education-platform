# --- Compiled binaries / executables (match by extension or output dir) ---
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# --- Build output directories (anchored so they only match repo-root dirs) ---
/bin/
/dist/
/build/

# --- Test & coverage artifacts ---
*.prof
*.log
coverage.txt
coverage.html

# --- Vendor directory (we use go.sum + module cache, not vendoring) ---
vendor/

# --- Go workspace files (enable only if using go work) ---
go.work
go.work.sum

# --- IDE / editor ---
.idea/
.vscode/
*.code-workspace

# --- OS files ---
.DS_Store
Thumbs.db
